# Revised Burst 2A Completion Report

Generation date: 2026-06-24

## 1. Starting Branch, Commit, and Working-Tree State

- Branch: `main`
- Commit at burst start: `ad55b67c13fbd143e2991dea003d85c3a4848706`
- Working tree at burst start: not clean
- Starting dirty state matched the expected post-Burst-1 source/test/config changes plus generated `__pycache__` artifacts from prior verification runs

## 2. Burst 1 Verification Result

Burst 1 was revalidated from the repository before editing.

Verified in code and/or tests:

- `inhouse_capture_target` remained removed
- `decision_policy.minimum_inhouse_coverage_target` existed
- default coverage target remained exactly `0.85`
- baseline, draft, applied, reset, and stale-result wiring remained intact
- recommendation candidates remained the full feasible integer range
- the coverage-constrained optimizer remained active
- the one-cent monetary tie rule remained active
- the no-eligible fallback remained active
- the saved Burst 1 completion report existed, was readable, and matched the repository
- the application still launched

Pre-edit full-suite rerun:

- Command: `python -m unittest discover -s tests`
- Result: `69/69` passed
- Match to Burst 1 report: yes

## 3. Intervening Changes, If Any

Intervening uncommitted changes present before Revised Burst 2A were the expected Burst 1 outputs and generated bytecode:

- Burst 1 source/config/test files remained modified but uncommitted
- `burst_0_repository_gap_analysis.md` remained untracked
- `decision_interaction_refinement_spec.md` remained untracked
- `docs/implementation_reports/burst_1_completion_report.md` remained present
- `__pycache__` files were present and continued to update during test and smoke runs

No repository inconsistency was found between the saved Burst 1 report and the actual code.

## 4. Files Modified

Direct Revised Burst 2A code/report edits:

- `src/constants.py`
- `src/finance/staffing_evaluator.py`
- `src/decision/narrative.py`
- `src/orchestration.py`
- `src/simulation/shortage.py`
- `tests/test_decision.py`
- `tests/test_end_to_end.py`
- `docs/implementation_reports/revised_burst_2a_completion_report.md`

Generated artifacts updated during verification:

- `__pycache__/...`
- `src/**/__pycache__/...`
- `tests/**/__pycache__/...`

## 5. Exact Manager-Proposal Examples

Current default config/data examples, using exact what-if evaluation with no clamping:

| Case | Staffing | Feasibility | Coverage | Overflow Prob. | Labor Cost | Overflow Comm. | Total Cost | Warning |
|---|---:|---|---:|---:|---:|---:|---:|---|
| Zero agents | `0` | `below_operating_floor` | `0.0000` | `1.0000` | `$0.00` | `$88,476.39` | `$88,476.39` | below-floor warning |
| Below floor | `7` | `below_operating_floor` | `0.0076` | `0.9924` | `$6,160.00` | `$28,870.08` | `$35,030.08` | below-floor warning |
| Within range | `10` | `within_operating_range` | `0.3924` | `0.6076` | `$8,800.00` | `$6,536.46` | `$15,336.46` | none |
| Above cap | `13` | `above_inhouse_capacity` | `0.9708` | `0.0292` | `$11,440.00` | `$133.95` | `$11,573.95` | above-cap warning |

Structured manager-proposal contract now returns:

- `staffing_agents`
- `feasibility_status`
- `capacity_confidence`
- `probability_overflow_required`
- `expected_spare_capacity_hours`
- `expected_overflow_workload_hours`
- `expected_overflow_bookings_by_category`
- `regular_labor_cost`
- `expected_overflow_commission`
- `expected_total_weekly_operating_cost`
- `warnings`

## 6. Previous-Week Staffing Examples

Current examples:

| Case | Staffing | Feasibility | Below Floor | Within Range | Above Cap |
|---|---:|---|---|---|---|
| Below floor | `7` | `below_operating_floor` | `True` | `False` | `False` |
| Within range | `9` | `within_operating_range` | `False` | `True` | `False` |
| Above cap | `13` | `above_inhouse_capacity` | `False` | `False` | `True` |

Previous-week staffing is now evaluated exactly for context and risk-cost records, but it does not constrain the recommendation optimizer.

## 7. Recommendation-Independence Result

Verified:

- changing only `manager_planned_staffing` does not change `recommendation_policy.minimum_inhouse_coverage_target`
- changing only `manager_planned_staffing` does not change the feasible recommendation candidate set
- changing only `manager_planned_staffing` does not change the Burst 1 optimizer rule
- out-of-range manager values are not inserted into the recommendation candidate set
- changing only `previous_week_staffing` does not change the recommendation result

Example:

- Manager `8` and manager `25` both preserved recommendation candidates `[8, 9, 10, 11, 12]`
- both preserved the same recommendation: `12` agents

## 8. Comparison-Object Example

For manager proposal `10` versus recommendation `12`, the backend comparison object is:

```python
{
    "staffing_difference": -2,
    "coverage_difference": -0.4886,
    "overflow_probability_difference": 0.4886,
    "labor_cost_difference": -1760.0,
    "overflow_commission_difference": 5845.0950227012345,
    "total_cost_difference": 4085.0950227012327,
    "spare_capacity_difference": -16.476380000000002,
    "overflow_workload_difference": 8.52362,
    "manager_feasibility_status": "within_operating_range",
}
```

Difference direction is now explicitly fixed as:

```text
manager value - recommendation value
```

All values come from shared backend staffing-evaluation records.

## 9. Narrative Examples

Adaptive backend narrative examples:

- Equal plans:
  `The manager proposal matches the model recommendation at 12 agents...`
- Below floor:
  `The manager proposal uses 5 fewer agents than the model recommendation (7 vs. 12)... The manager proposal is below the operating floor...`
- Above cap:
  `The manager proposal uses 1 more agents than the model recommendation (13 vs. 12)... The manager proposal is above the in-house capacity cap...`
- Unachievable target:
  `The configured in-house coverage target is not achievable within the feasible in-house range, so the model recommendation falls back to the maximum feasible staffing level.`

The new structured narrative object returns:

- `text`
- `warnings`
- `difference_direction`

Warnings come from structured manager/recommendation records and do not reconstruct costs.

## 10. Risk-Cost Staffing Levels and Flags

Risk-cost record set now uses:

- every feasible recommendation level
- exact manager staffing
- exact previous-week staffing
- deduplicated
- sorted ascending

Example with manager `0` and previous week `13`:

```python
[
    (0, False, True, False),
    (8, False, False, False),
    (9, False, False, False),
    (10, False, False, False),
    (11, False, False, False),
    (12, True, False, False),
    (13, False, False, True),
]
```

Tuple meaning:

```text
(staffing_agents, is_model_recommendation, is_manager_proposal, is_previous_week)
```

Each structured risk-cost record now returns:

- `staffing_agents`
- `feasibility_status`
- `is_model_recommendation`
- `is_manager_proposal`
- `is_previous_week`
- `capacity_confidence`
- `probability_overflow_required`
- `expected_spare_capacity_hours`
- `expected_overflow_workload_hours`
- `expected_overflow_bookings_by_category`
- `regular_labor_cost`
- `expected_overflow_commission`
- `expected_total_weekly_operating_cost`

## 11. Probabilistic Invariant Result

Preserved and reverified:

```text
capacity_confidence + probability_overflow_required ~= 1.0
```

Also preserved:

```python
expected_spare_capacity_hours = mean(
    max(capacity_hours - simulated_workload_hours, 0)
)

expected_overflow_workload_hours = mean(
    max(simulated_workload_hours - capacity_hours, 0)
)
```

Zero-agent what-if evaluation required one numerical-stability fix in `src/simulation/shortage.py`: overflow bookings are now bounded to `[0, demand]` before computing in-house bookings so floating-point noise cannot create tiny negative booking counts.

## 12. Focused Test Commands and Results

Syntax check:

- Command: `python -m py_compile src/constants.py src/finance/staffing_evaluator.py src/decision/narrative.py src/orchestration.py tests/test_end_to_end.py tests/test_decision.py src/simulation/shortage.py`
- Result: passed

Focused tests:

- Command: `python -m unittest tests.test_decision tests.test_end_to_end tests.test_simulation tests.test_finance tests.test_ui_bundle`
- Result: `68/68` passed

Observed warnings:

- `No runtime found, using MemoryCacheStorageManager`

## 13. Full-Suite Command and Result

- Command: `python -m unittest discover -s tests`
- Result: `80/80` passed

Observed warnings:

- `No runtime found, using MemoryCacheStorageManager`

## 14. Streamlit Smoke-Test Command and Result

Commands run:

```text
python -m streamlit run app.py --server.headless true --server.port 8501
Invoke-WebRequest -UseBasicParsing -Uri 'http://127.0.0.1:8501/_stcore/health' -TimeoutSec 10
Invoke-WebRequest -UseBasicParsing -Uri 'http://127.0.0.1:8501/' -TimeoutSec 10
```

Result:

- Streamlit launched successfully
- health endpoint returned HTTP `200` with `ok`
- root page returned HTTP `200`
- no startup exception appeared in the server output

## 15. Temporary Compatibility Behavior

The new authoritative backend objects now exist, but legacy compatibility outputs remain in place intentionally:

- `financial_recommendation` is still returned
- `staffing_evaluation_table` is still returned
- `narrative.text`, `narrative.warnings`, and `narrative.comparison_table` are still returned
- `narrative.manager_comparison` now mirrors the new adaptive comparison narrative
- the single-page dashboard structure remains unchanged
- the temporary Burst 1 decision-policy UI control remains unchanged
- no final risk-cost table UI was added in this burst

## 16. Deviations or Unresolved Issues

No functional deviations from the approved Revised Burst 2A scope were required.

Known temporary state:

- the working tree remains intentionally not clean because Burst 1 outputs were never committed
- generated `__pycache__` files changed during verification
- final UI redesign, percentile outlooks, scenario retirement, and manual override repair remain intentionally deferred

## 17. GPT-5.4 Mini Subagents Used and Their Exact Bounded Scopes

None.

## 18. Public Result Contracts Added or Changed

New authoritative top-level result objects:

- `recommendation_policy`
- `recommended_plan`
- `manager_proposal`
- `previous_week_staffing_context`
- `recommendation_manager_comparison`
- `adaptive_comparison_narrative`
- `staffing_risk_cost_records`

Key behavior changes:

- manager and previous-week staffing are evaluated exactly even when outside the feasible operating range
- feasible recommendation candidates remain only the inclusive feasible integer range
- out-of-range manager and previous-week values do not alter the Burst 1 optimizer
- feasibility status values are now explicit:
  - `below_operating_floor`
  - `within_operating_range`
  - `above_inhouse_capacity`

## 19. Compaction Handoff for Revised Burst 2B

Revised Burst 2B should start from the repository, this report, and the authoritative corrected spec file.

Immediate repository facts:

- Branch: `main`
- Commit: `ad55b67c13fbd143e2991dea003d85c3a4848706`
- Working tree clean: no

Expected non-clean outputs:

- retained Burst 1 source/config/test changes
- Revised Burst 2A changes in:
  - `src/constants.py`
  - `src/finance/staffing_evaluator.py`
  - `src/decision/narrative.py`
  - `src/orchestration.py`
  - `src/simulation/shortage.py`
  - `tests/test_decision.py`
  - `tests/test_end_to_end.py`
  - `docs/implementation_reports/revised_burst_2a_completion_report.md`
- generated `__pycache__` updates from test and smoke execution

Authoritative contracts created or changed:

- `manager_proposal` exact what-if evaluation contract
- `previous_week_staffing_context` exact feasibility-context contract
- `recommendation_manager_comparison` with difference direction `manager value - recommendation value`
- `adaptive_comparison_narrative`
- `staffing_risk_cost_records`
- `recommendation_policy` as the explicit optimizer-policy object
- `recommended_plan` as the explicit structured recommendation object

Exact public result fields added:

- `recommendation_policy`
- `recommended_plan`
- `manager_proposal`
- `previous_week_staffing_context`
- `recommendation_manager_comparison`
- `adaptive_comparison_narrative`
- `staffing_risk_cost_records`

Manager-proposal behavior:

- accepts exact nonnegative integer staffing levels
- evaluates below-floor and above-cap values without clamping
- returns `warnings` for below-floor and above-cap values
- does not enter out-of-range manager levels into the recommendation candidate set
- does not change Burst 1 optimizer behavior

Comparison difference direction:

```text
manager value - recommendation value
```

Feasibility status values:

- `below_operating_floor`
- `within_operating_range`
- `above_inhouse_capacity`

Staffing risk-cost record structure:

- `staffing_agents`
- `feasibility_status`
- `is_model_recommendation`
- `is_manager_proposal`
- `is_previous_week`
- `capacity_confidence`
- `probability_overflow_required`
- `expected_spare_capacity_hours`
- `expected_overflow_workload_hours`
- `expected_overflow_bookings_by_category`
- `regular_labor_cost`
- `expected_overflow_commission`
- `expected_total_weekly_operating_cost`

Tests protecting these contracts:

- `tests.test_end_to_end.ApplicationOrchestrationEndToEndTests`
- `tests.test_decision.DecisionTests`
- existing `tests.test_simulation` invariant tests

Temporary adapters still present:

- legacy `financial_recommendation`
- legacy `staffing_evaluation_table`
- legacy `narrative.comparison_table`
- legacy `narrative.text` and `narrative.warnings`
- `narrative.manager_comparison` compatibility mirror

Known unresolved issues:

- percentile outlook implementation still not started
- final UI redesign still not started
- final risk-cost table UI still not started
- scenario retirement still not started
- manual override repair still not started

Files Revised Burst 2B is expected to modify:

- `src/orchestration.py`
- `src/decision/narrative.py`
- `src/ui/components.py`
- `src/ui/charts.py`
- possibly `src/ui/state.py`
- related tests

Files Revised Burst 2B should avoid changing unless required by verified defect:

- `src/decision/optimizer.py`
- `src/decision/plans.py`
- forecasting and Monte Carlo core modules
- scenario retirement code paths unless the burst explicitly includes that scope

Deviations from this prompt:

- none functional

Path to this completion report:

```text
docs/implementation_reports/revised_burst_2a_completion_report.md
```

## COMPACTION HANDOFF FOR REVISED BURST 2B

Current branch: `main`

Current commit: `ad55b67c13fbd143e2991dea003d85c3a4848706`

Working tree clean: no

Expected working-tree outputs of Burst 1 and Revised Burst 2A:

- Burst 1 uncommitted source/config/test/report/spec outputs remain present
- Revised Burst 2A source changes:
  - `src/constants.py`
  - `src/finance/staffing_evaluator.py`
  - `src/decision/narrative.py`
  - `src/orchestration.py`
  - `src/simulation/shortage.py`
  - `tests/test_decision.py`
  - `tests/test_end_to_end.py`
- persisted report:
  - `docs/implementation_reports/revised_burst_2a_completion_report.md`
- generated `__pycache__` updates are expected from verification commands

Authoritative contracts created or changed:

- top-level `recommendation_policy`
- top-level `recommended_plan`
- top-level `manager_proposal`
- top-level `previous_week_staffing_context`
- top-level `recommendation_manager_comparison`
- top-level `adaptive_comparison_narrative`
- top-level `staffing_risk_cost_records`

Exact public result fields added:

- `recommendation_policy`
- `recommended_plan`
- `manager_proposal`
- `previous_week_staffing_context`
- `recommendation_manager_comparison`
- `adaptive_comparison_narrative`
- `staffing_risk_cost_records`

Manager-proposal behavior:

- exact evaluation for any nonnegative integer staffing level
- no clamping for below-floor or above-cap proposals
- below-floor and above-cap proposals receive feasibility warnings
- manager levels outside the feasible range do not enter the recommendation candidate set
- changing only manager staffing does not alter the Burst 1 recommendation optimizer

Comparison difference direction:

```text
manager value - recommendation value
```

Feasibility status values:

- `below_operating_floor`
- `within_operating_range`
- `above_inhouse_capacity`

Staffing risk-cost record structure:

- `staffing_agents`
- `feasibility_status`
- `is_model_recommendation`
- `is_manager_proposal`
- `is_previous_week`
- `capacity_confidence`
- `probability_overflow_required`
- `expected_spare_capacity_hours`
- `expected_overflow_workload_hours`
- `expected_overflow_bookings_by_category`
- `regular_labor_cost`
- `expected_overflow_commission`
- `expected_total_weekly_operating_cost`

Tests protecting those contracts:

- manager out-of-range evaluation and recommendation independence in `tests/test_end_to_end.py`
- previous-week out-of-range context and risk-cost inclusion in `tests/test_end_to_end.py`
- comparison difference direction in `tests/test_end_to_end.py`
- adaptive narrative branches in `tests/test_decision.py`
- probabilistic complement invariant in `tests/test_simulation.py`

Temporary adapters still present:

- legacy `financial_recommendation`
- legacy `staffing_evaluation_table`
- legacy `narrative.text`
- legacy `narrative.warnings`
- legacy `narrative.comparison_table`
- compatibility mirror `narrative.manager_comparison`

Known unresolved issues:

- no percentile demand outlook objects yet
- no final visible staffing risk-cost table UI yet
- no final Business Decisions vs Business Assumptions layout yet
- no scenario retirement yet
- no manual override interaction repair yet

Files Revised Burst 2B is expected to modify:

- `src/ui/components.py`
- `src/ui/charts.py`
- `src/orchestration.py`
- `src/decision/narrative.py`
- related tests

Files Revised Burst 2B should avoid changing:

- `src/decision/optimizer.py`
- `src/decision/plans.py`
- simulation/forecasting core modules unless a verified defect requires it

Deviations from the Revised Burst 2A prompt:

- none functional

Path to this completion report:

```text
docs/implementation_reports/revised_burst_2a_completion_report.md
```
